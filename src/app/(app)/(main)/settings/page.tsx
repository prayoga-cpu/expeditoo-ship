import { Settings } from "@/features/app/profile/ui";

/**
 * Settings page - Orchestration layer
 * Following rules.md - clean page with no business logic
 */
export default function SettingsPage() {
  return <Settings />;
}
