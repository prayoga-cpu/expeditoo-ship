import { Checkout } from "@/features/app/checkout/ui";

/**
 * Checkout page - Orchestration layer
 * Following rules.md - clean page with no business logic
 */
export default function CheckoutPage() {
  return <Checkout />;
}
