import { PlatformSettingsPanel } from "@/features/app/platform-settings/ui";

/** The platform fee rate, admin/finance only. */
export default function AdminSettingsPage() {
  return <PlatformSettingsPanel />;
}
