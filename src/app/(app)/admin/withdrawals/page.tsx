import { WithdrawalQueue } from "@/features/app/withdrawals/ui";

/** The operator queue: who is asking for money, and the three answers. */
export default function AdminWithdrawalsPage() {
  return <WithdrawalQueue />;
}
